# Logbook — 2026-05-25

## Contexto

Dia dedicado à consolidação da S3 — Domain Layer, com foco em calibração probabilística, entidades, value objects, ports/protocols e validação de qualidade via testes, type-checking e CI.

## Entregas realizadas

### S3 — Domain Layer

- Implementado `src/bci_calib/domain/calibration.py`
  - `CalibrationParams` frozen
  - sigmoid numericamente estável
  - `compute_tau(r_t, params)`
  - validação exata: `compute_tau(0.0, CalibrationParams(3.0, 1.0)) == 2.0`

- Implementados objetos e entidades de domínio:
  - `AblationCondition` com valores A–G
  - `Epoch`
  - `RRInterval`
  - `Participant`
  - `SessionMetadata`
  - campo `functionally_identifiable: bool`

- Implementados ports/protocols:
  - `EEGSource`
  - `HRVSource`
  - `MIClassifier`
  - `Calibrator`

## Correções realizadas

- Ajustes de compatibilidade com `mypy 2.0`
- Remoção de `# type: ignore[misc]` órfãos em decorators Hypothesis
- Correção de conflitos em `pyproject.toml`
- Ajustes finais de formatação e documentação

## Validação

- `pytest tests/unit/domain`: PASS
- Coverage individual dos arquivos de `domain/`: 100%
- `mypy src/bci_calib/domain --strict`: PASS
- `ruff check`: PASS
- `bandit -r src/bci_calib/domain`: PASS
- Zero imports proibidos em `domain/`

## Status

S3 validada tecnicamente em nível de domínio.

## Próximos passos

- Promover `dev` para `main`
- Registrar fechamento da S3
- Iniciar próxima etapa após confirmação do CI verde
